<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".DirectorDetailActivity"
    android:padding="20dp"
    android:orientation="vertical">

    <RatingBar
        android:id="@+id/directorAddRatingStars"
        android:layout_marginTop="20dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:numStars="5"
        android:rating="0" />

    <EditText
        android:id="@+id/directorAddRatingText"
        android:layout_marginTop="10dp"
        android:inputType="textMultiLine"
        android:lines="8"
        android:maxLines="12"
        android:gravity="top|left"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:scrollbars="vertical"
        android:hint="Vložte text vaší recenze"/>

    <Button
        android:id="@+id/directorAddRating"
        android:layout_marginTop="50dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Vložit recenzi"/>
</LinearLayout>